import React from "react";
import styled from "styled-components";

export default function TopToolbarLoadingSpinner() {
  return <Loading>더미</Loading>;
}

const Loading = styled.div`
  height: 11.5rem;
  color: transparent;
`;
